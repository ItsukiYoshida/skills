interface:
  display_name: "Video Context Extractor"
  short_description: "Extract context from video files and YouTube URLs"
  default_prompt: "Use $video-context-extractor to inspect this video or YouTube URL and add the relevant visual context."
